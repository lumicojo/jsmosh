//Write a function that takes 2 num and return the maximum of 2.
// called the function with a diferent argument
// before we declare a function
// we declare a variable called number and set it to max of 1 and 2
let number = max(1, 2);
// display number in the conso;e
console.log(number)
// we declare a function called max with 2 param
function max(a, b) {
  //if a is gretar than b will return a
  if (a > b) return a;
  // otherwise return b
  else
    return b;
}



/// or

  let num = max(1, 2);
  function max(a, b) {
    return (a > b) ? a: b;
  }



