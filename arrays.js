let selectedColors = ['red', 'blue'];
//we add another elem to the array
selectedColors[2] = 'green'; // is dinamic
selectedColors[2] = 1;

console.log(selectedColors [0]); // red
console.log(selectedColors ); // 3 elem
console.log(selectedColors.length ); // 3
