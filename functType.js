// perform task
function greet(name, lastName) {
  console.log('hello ' + name + ' ' + lastName);
}
greet('carmen', 'weiver')

// calculate value
function square(number) {
  return number * number;
}
//square(2); // this return value so we can use the value to initialize the veriable
//let number = square(2);
console.log(square(2));
